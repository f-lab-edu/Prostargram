<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="flab.project.domain.user.mapper.SignUpMapper">

    <insert id="addUser">
        INSERT INTO USERS(email, password, user_name, login_type)
            VALUES (#{email}, #{password}, #{userName}, #{loginType});
    </insert>

</mapper>