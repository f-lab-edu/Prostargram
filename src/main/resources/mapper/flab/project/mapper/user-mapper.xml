<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="flab.project.domain.user.mapper.UserMapper">

  <select id="findAll" parameterType="flab.project.domain.user.enums.GetFollowsType"
    resultType="flab.project.domain.user.model.User">
    SELECT
    U.user_id,
    U.user_name,
    U.profile_img_url,
    O.name
    FROM USERS AS U
    JOIN ORGANIZATION O on U.organization_id = O.organization_id
    <choose>
      <when test='requestType.name().equals("FOLLOWERS")'>
        WHERE user_id IN (
        SELECT
        from_user_id
        FROM FOLLOWS
        WHERE to_user_id=#{userId}
        );
      </when>
      <when test='requestType.name().equals("FOLLOWINGS")'>
        WHERE user_id IN (
        SELECT
        to_user_id
        FROM FOLLOWS
        WHERE from_user_id=#{userId}
        );
      </when>
    </choose>
  </select>

  <insert id="updateProfileImage">
    UPDATE USERS
    SET profile_img_url=#{profileImgUrl}
    WHERE user_id = #{userId};
  </insert>

  <select id="getProfileUpdatePageInfo" resultMap="profileUpdatePageResult">
    SELECT U.user_id,
    U.user_name,
    U.profile_img_url,
    U.organization_information,
    U.self_introduction,
    U.post_count,
    S.social_account_id,
    S.link,
    L.icon_url,
    H.hashtag_id,
    H.name
    FROM USERS U
    LEFT JOIN SOCIAL_ACCOUNTS S on U.user_id = S.user_id
    LEFT JOIN LINK_ICON L on S.icon_id = L.icon_id
    LEFT JOIN INTERESTS I on u.user_id = I.user_id
    LEFT JOIN HASHTAGS H on I.hashtag_id = H.hashtag_id
    WHERE U.user_id = #{userId};
  </select>

  <select id="getProfilePageInfo" resultMap="profilePageResult">
    SELECT U.user_id,
    U.user_name,
    U.profile_img_url,
    U.organization_information,
    U.self_introduction,
    U.post_count,
    U.follower_count,
    U.following_count,
    IFNULL(follow.to_user_id, FALSE) as is_follow,
    S.social_account_id,
    S.link,
    L.icon_url,
    H.hashtag_id,
    H.name
    FROM USERS U
    LEFT JOIN SOCIAL_ACCOUNTS S on U.user_id = S.user_id
    LEFT JOIN LINK_ICON L on S.icon_id = L.icon_id
    LEFT JOIN INTERESTS I on u.user_id = I.user_id
    LEFT JOIN HASHTAGS H on I.hashtag_id = H.hashtag_id
    LEFT JOIN FOLLOWS follow
              on follow.from_user_id = #{myUserId} and follow.to_user_id = #{targetUserId}
    WHERE U.user_id = #{targetUserId};
  </select>

  <select id="getBasicUser" resultType="BasicUser">
    SELECT user_id,
           user_name,
           profile_img_url
    FROM USERS
    WHERE user_id = #{userId};
  </select>

  <select id="findWhereUserIdIn" resultType="Profile">
    SELECT U.user_id,
    U.user_name,
    U.profile_img_url,
    U.organization_information,
    U.self_introduction,
    U.post_count,
    U.follower_count,
    U.following_count,
    S.social_account_id,
    S.link,
    L.icon_url,
    H.hashtag_id,
    H.name
    FROM USERS U
    LEFT JOIN SOCIAL_ACCOUNTS S on U.user_id = S.user_id
    LEFT JOIN LINK_ICON L on S.icon_id = L.icon_id
    LEFT JOIN INTERESTS I on u.user_id = I.user_id
    LEFT JOIN HASHTAGS H on I.hashtag_id = H.hashtag_id
    WHERE U.user_id in
    <foreach collection="userIds" item="userId" index="index" open="(" close=")" separator=",">
      #{userId}
    </foreach>
  </select>

  <select id="existsByEmail" resultType="boolean">
    SELECT COUNT(1) > 0
    FROM USERS
    WHERE email = #{email}
  </select>

  <select id="existsByUsername" resultType="boolean">
    SELECT COUNT(1) > 0
    FROM USERS
    WHERE user_name = #{username}
  </select>

  <resultMap id="profilePageResult" type="ProfilePage">
    <id property="userId" column="user_id"/>
    <result property="userName" column="user_name"/>
    <result property="profileImgUrl" column="profile_img_url"/>
    <result property="departmentName" column="organization_information"/>
    <result property="selfIntroduction" column="self_introduction"/>
    <result property="follow" column="is_follow"/>
    <result property="postCount" column="post_count"/>
    <result property="followerCount" column="follower_count"/>
    <result property="followingCount" column="following_count"/>
    <association property="socialAccounts" resultMap="socialAccountResult"/>
    <association property="interests" resultMap="hashtagResult"/>
  </resultMap>

  <resultMap id="profileUpdatePageResult" type="Profile">
    <id property="userId" column="user_id"/>
    <result property="userName" column="user_name"/>
    <result property="profileImgUrl" column="profile_img_url"/>
    <result property="departmentName" column="organization_information"/>
    <result property="selfIntroduction" column="self_introduction"/>
    <result property="postCount" column="post_count"/>
    <association property="socialAccounts" resultMap="socialAccountResult"/>
    <association property="interests" resultMap="hashtagResult"/>
  </resultMap>

  <resultMap id="socialAccountResult" type="SocialAccountResponse">
    <id property="socialAccountId" column="social_account_id"/>
    <result property="iconUrl" column="icon_url"/>
    <result property="socialAccountUrl" column="link"/>
  </resultMap>

  <resultMap id="hashtagResult" type="HashTag">
    <id property="hashTagId" column="hashtag_id"/>
    <result property="hashTagName" column="name"/>
  </resultMap>

  <update id="updateProfile" parameterType="flab.project.domain.user.model.UpdateProfileRequestDto">
    UPDATE USERS
    <set>
      <if test="updateProfileRequestDto.userName !=null">
        user_name=#{updateProfileRequestDto.username},
      </if>
      <if test="updateProfileRequestDto.departmentName !=null">
        organization_information=#{updateProfileRequestDto.departmentName},
      </if>
      <if test="updateProfileRequestDto.selfIntroduction !=null">
        self_introduction=#{updateProfileRequestDto.selfIntroduction}
      </if>
    </set>
    WHERE user_id=#{userId};
  </update>

</mapper>